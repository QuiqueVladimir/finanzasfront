import { UserPortfoliosEntity } from './user-portfolios-entity';

describe('UserPortfoliosEntity', () => {
  it('should create an instance', () => {
    expect(new UserPortfoliosEntity()).toBeTruthy();
  });
});
